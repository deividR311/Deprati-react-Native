import { COLORS } from '../common/colors';

export const backgroundInputState = {
  normal: COLORS.WHITE,
  error: COLORS.ERRORBACKGROUND
};

export const borderColorInputState = {
  normal: COLORS.DARK70,
  error: COLORS.ERRORBACKGROUND
};
